[pytest]
testpaths = agent/tests
addopts = -m "not external and not e2e"
markers =
    integration: 로컬 DB나 HTTP 계층을 함께 검증하는 통합 테스트
    external: 실제 외부 LLM API를 호출하는 테스트
    e2e: 브라우저, 화면, OCR, 물리 입력을 사용하는 종단 테스트
